# Insertion Sort
def ins(x):
    for i in range(1,len(a)):
        key=x[i]
        j=i-1
        while j>=0 and x[j]>key:
            x[j+1]=x[j]
            j-=1
        x[j+1]=key
    return x 
n=int(input("Enter number a elements in a list: \n"))
a=list(map(int,input("Enter all numbers in a list seprated by space for sorting: \n").split()))
b=ins(a)
print(b)
   
            
